<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/routes"
    app:startDestination="@id/navigationMenu">

    <fragment
        android:id="@+id/navigationMenu">
        <action
            android:id="@+id/action_navigationMenu_to_personalAccount"
            app:destination="@id/personalAccount" />
    </fragment>
    <fragment
        android:id="@+id/personalAccount"
        android:name="com.example.healthystate.Navigations.PersonalAccount"
        android:label="Личный аккаунт"
        tools:layout="@layout/fragment_personal_account" />
</navigation>